#!/bin/sh
# Block commits that change one agent bundle without its mirror.
# Enable once per clone:  git config core.hooksPath .githooks
scripts/check-bundle-sync.sh --staged || exit 1
exec scripts/test-adapters.sh
