# Generated by scripts/gen-adapters.sh — do not edit by hand.
description = "Resume an inbox entry through to backlog (or close as fold/wontfix). Requires a stage with the `inbox` role."
prompt = """
Read the file `.agents/skills/ticket-refine/SKILL.md` and follow it exactly, start to finish.

That skill is the single source of truth for this workflow. This command only
routes to it — never re-implement, summarise, or shortcut a step from memory.
The skill's gates are numbered lists: print the options and wait for the user's
number. Never pick an option for them.

The user's starting input ("[optional inbox ticket ID]"): {{args}}
"""
