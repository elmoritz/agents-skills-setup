# Generated by scripts/gen-adapters.sh — do not edit by hand.
description = "Bootstrap a project for the /ticket-* workflow. Writes .agents/config.yaml, creates stage folders (with ledger) or workflow labels/fields, guides research-agent setup, and lays down a starter TICKET_TEMPLATE.md."
prompt = """
Read the file `.agents/skills/ticket-init/SKILL.md` and follow it exactly, start to finish.

That skill is the single source of truth for this workflow. This command only
routes to it — never re-implement, summarise, or shortcut a step from memory.
The skill's gates are numbered lists: print the options and wait for the user's
number. Never pick an option for them.

The user's starting input ((no arguments; interactive)): {{args}}
"""
