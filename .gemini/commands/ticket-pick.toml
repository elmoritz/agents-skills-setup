# Generated by scripts/gen-adapters.sh — do not edit by hand.
description = "Pick the next ticket from the pickable stage and implement it through to review (or directly to closure-ready, depending on whether a review stage exists)."
prompt = """
Read the file `.agents/skills/ticket-pick/SKILL.md` and follow it exactly, start to finish.

That skill is the single source of truth for this workflow. This command only
routes to it — never re-implement, summarise, or shortcut a step from memory.
The skill's gates are numbered lists: print the options and wait for the user's
number. Never pick an option for them.

The user's starting input ("[optional ticket ID to pick directly; otherwise top 4 are surfaced]"): {{args}}
"""
