ok=false
where=validation
failed=review.agents: 'ghost-reviewer' does not resolve to an agent file in the agents directory
recovery=fix tests/fixtures/config/r17-missing-review/config.yaml per the message, then re-run — the engine never proceeds past a config error
--- exit: 1
